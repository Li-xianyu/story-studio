{
  "name": "story-studio-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker sync backend for Story Studio PWA",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv namespace create SYNC_KV",
    "kv:create-preview": "wrangler kv namespace create SYNC_KV --preview"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
